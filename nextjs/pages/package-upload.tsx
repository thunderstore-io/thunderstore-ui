import { PackageUpload } from "thunderstore-components";
import React from "react";

const PackageUploadPage: React.FC<{}> = ({}) => {
  return <PackageUpload />;
};

export default PackageUploadPage;
