// Variants
export const ModalVariantsList = ["default"] as const;
export type ModalVariants = "default";

// Sizes
export const ModalSizesList = ["medium", "small"] as const;
export type ModalSizes = "medium" | "small";
